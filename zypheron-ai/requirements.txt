# Core AI Libraries
anthropic~=0.42.0
openai~=1.58.0
google-generativeai~=0.8.0
requests~=2.32.0

# ML & Data Science
# Floors raised to first releases shipping cp314 wheels; older minors are
# sdist-only and fail to compile on Python 3.14 (Arch). pandas held <3 (2.3.x)
# to avoid the pandas 3.0 API break.
numpy~=2.2
pandas~=2.3
scikit-learn~=1.7
# torch/transformers are optional and very large; not required for core AI engine startup

# Security & Pentesting
python-nmap~=0.7.1
scapy~=2.6.0
# pwntools/unicorn are optional and may not build on Python 3.14 without system build tools.
# If you need exploitation tooling, install from a compatible Python (3.11/3.12) or add build deps.
beautifulsoup4~=4.12.0

# CVE & Vulnerability Data
nvdlib~=0.7.4
cvelib~=1.6.0

# Graph Analysis
networkx~=3.4.0
matplotlib~=3.10  # 3.9.x has no cp314 wheel

# Natural Language Processing
spacy~=3.8.0
nltk~=3.9.0

# IPC & Communication
pyzmq~=27.0  # 26.2.x has no cp314 wheel

# Utilities
python-dotenv~=1.0.0
pydantic~=2.11  # 2.10.x pins pydantic-core 2.27 (no cp314 wheel)
pydantic-settings~=2.7.0
pyyaml~=6.0.1
colorama~=0.4.6
rich~=13.9.0
tenacity~=9.0.0

# Secure credential storage
keyring~=25.5.0
cryptography~=44.0.0

# Async support
aiohttp~=3.12  # 3.11.x has no cp314 wheel

# Local LLM support (Ollama)
ollama~=0.4.0

# DeepSeek support
# (via OpenAI-compatible API)

# Logging
loguru~=0.7.0
